{
  "name": "bank-webhook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@repo/db": "*",
    "@repo/webhook-signing": "*",
    "@types/express": "^5.0.3",
    "cors": "^2.8.5",
    "esbuild": "^0.25.9",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "winston": "^3.17.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19"
  }
}